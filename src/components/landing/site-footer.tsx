export function SiteFooter() {
  return (
    <footer className="relative z-10 mx-auto flex w-full max-w-6xl flex-col items-center justify-between gap-3 border-t border-border px-6 py-8 text-xs text-foreground/35 sm:flex-row sm:px-8">
      <span>Vantaverse AI Builder Lab · Founding Builders — Cohort 01</span>
      <span>A place for curious people to become builders.</span>
    </footer>
  );
}
